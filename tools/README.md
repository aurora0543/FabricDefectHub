# tools

数据转换、导出和可视化脚本（非包代码，独立运行）。计划中：

- `convert_annotations.py` — 第三方标注格式 -> `Sample` JSON。
- `export_model.py` — 调用 `ModelAdapter.export` 的命令行入口。
- `visualize_predictions.py` — 在图像上绘制 `Prediction`（框/掩码/异常热力图）。
